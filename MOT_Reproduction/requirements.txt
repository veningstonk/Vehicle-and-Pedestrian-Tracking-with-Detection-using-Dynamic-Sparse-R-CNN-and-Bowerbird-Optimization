# requirements.txt
# MEAS-D-25-13753 Reproduction Pipeline
# Hardware: Intel Core i7-14700K + NVIDIA GeForce RTX 4060 (8 GB VRAM)
# Install order matters: torch first, then the rest.
#
# STEP 1 — Install PyTorch with CUDA 12.1 (RTX 4060 requires CUDA 11.8+):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# STEP 2 — Install everything else:
#   pip install -r requirements.txt

# Core deep learning
timm>=0.9.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
pandas>=2.0.0
matplotlib>=3.7.0

# Computer vision
opencv-python>=4.8.0

# MOT metrics (MOTA, IDF1)
motmetrics>=1.4.0

# Hungarian assignment / tracking helpers
filterpy>=1.4.5
lap>=0.4.0

# Progress bars
tqdm>=4.65.0
